README
{
    High priority
    {
        locale API
        game objects
        ui API
    }
    Medium priority
    {
        engine.cpp
        main.cpp
        compiler
        save/load
    }
    Low priority
    {
        Performance debugging
        Build folder
    }
}

Map
{
    Info will be stored in 2D array of unique id's of tiles

    Cliffs and other impassable things
    {
        Store hitboxes seperate or generate dynamically by using a file that describes hitboxes of tiles.
    }

    Runtime info
    {
        Calculate only when requested
        {
            Tile at position (get tile where position is within borders)
            Tile at col, row
        }
    }
}